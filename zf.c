#include "types.h"
#include "user.h"

int main(int argc, char** argv) {
  /*
  int pc = 0;
  int rc;

  for(int i = 0; i < 50; i++) {
    rc = fork();
    if (rc < 0) {
      printf(2, "Fork failed!\n");
      break;

    } else if (rc == 0) {
      printf(1, "Proc created. PID: %d\n", getpid());
      // Loop forever in child
      for (;;)
        continue;
    }
    pc++;
  }


  printf(1, "Procs created: %d", pc);
  //printf(1, "Fork created. PID: %d\n", rc);
  printf(1, "Killing soon\n");
  sleep(7000);
  kill(rc);
  printf(1, "KILLED.");
  sleep(10000);
  wait();
  sleep(10000);
   */

  exit();
}